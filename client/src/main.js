import React from "react";
import ReactDOM from 'react-dom/client';

const rootNode = document.getElementById('root');
ReactDOM.createRoot(rootNode).render(
  <React.StrictMode>
    <div>
      이제야 마켓컬리 클론 사이트 만들 준비가 되었습니다. git update를 해볼까요!!!
    </div>
  </React.StrictMode>,
);